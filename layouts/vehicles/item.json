{
    "name": "{{ .Title }}",
    "body": "{{ .Params.body }}",
    "segment": "{{ .Params.segment }}",
    "permalink" : "{{ .Permalink }}"
    {{ range $index, $e := .Site.Taxonomies }}
    	{{ with $.Param $index }}
    		,"{{ $index }}" : "{{ delimit . ", "}}"
    	{{ end }}
    {{ end }}  	
}